# --- Supabase ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Project Settings -> API -> JWT Secret (HS256). If the project uses asymmetric keys, see spec §8 (JWKS).
SUPABASE_JWT_SECRET=
# --- Admin ---
ADMIN_EMAIL=
# --- CORS: frontend origin(s), comma-separated ---
ALLOWED_ORIGINS=http://localhost:5173
